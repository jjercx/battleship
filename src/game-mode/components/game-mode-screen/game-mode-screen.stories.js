import React from "react";
import { GameModeScreen } from "./game-mode-screen";

export default {
  title: "game-mode/GameModeScreen",
  component: GameModeScreen,
};

export const Default = () => <GameModeScreen />;
